// Register Page
// - Form: name, email, password, role (provider / seeker)
// - Submit calls POST /api/auth/register
// - Redirect to Login on success

function Register() {
  return null;
}

export default Register;
